// Use 'export type' for TypeScript types/interfaces
export type NavLink = {
  label: string;
  href: string;
};

export type Service = {
  icon: string;
  title: string;
  desc: string;
};

export type Product = {
  label: string;
  icon: string;
};